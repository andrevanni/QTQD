const storageKey="qtqd_cliente_demo_v1";
const FIELD_CONFIG_KEY="qtqd_field_config_v1";
const THEME_KEY="qtqd_cliente_theme";
const BRANDING_KEY="qtqd_branding_v1";
const defaultBranding={clientName:"Cliente Demonstração",clientLogoUrl:""};
const componentLabels=[["saldo_bancario","Saldo bancário"],["contas_receber","Contas a receber"],["cartoes","Cartões"],["convenios","Convênios"],["cheques","Cheques"],["trade_marketing","Trade marketing"],["outros_qt","Outros QT"],["estoque_custo","Estoque custo"],["contas_pagar","Contas a pagar"],["fornecedores","Fornecedores"],["investimentos_assumidos","Investimentos assumidos"],["outras_despesas_assumidas","Outras despesas assumidas"],["dividas","Dívidas"],["financiamentos","Financiamentos"],["tributos_atrasados","Tributos atrasados"],["acoes_processos","Ações e processos"],["faturamento_previsto_mes","Faturamento previsto no mês"],["compras_mes","Compras no mês"],["entrada_mes","Entrada no mês"],["venda_cupom_mes","Venda cupom no mês"],["venda_custo_mes","Venda custo no mês - CMV"],["lucro_liquido_mes","Lucro líquido no mês"]];
const defaultFieldConfig=Object.fromEntries(componentLabels.map(([k,l])=>[k,{label:l,visible:true}]));
const matrixRows=[{key:"qt_total",label:"QT (QUANTO TENHO)",format:"currency",rowClass:"row-header"},{key:"saldo_bancario",label:"SALDO BANCÁRIO",format:"currency"},{key:"contas_receber",label:"CONTAS A RECEBER",format:"currency"},{key:"cartoes",label:"CARTÕES",format:"currency"},{key:"convenios",label:"CONVÊNIOS",format:"currency"},{key:"cheques",label:"CHEQUES",format:"currency"},{key:"trade_marketing",label:"TRADE MARKETING",format:"currency"},{key:"outros_qt",label:"OUTROS QT",format:"currency"},{key:"estoque_custo",label:"ESTOQUE CUSTO",format:"currency"},{type:"empty"},{key:"qd_total",label:"QD (QUANTO DEVO)",format:"currency",rowClass:"row-header"},{key:"contas_pagar",label:"CONTAS A PAGAR",format:"currency"},{key:"fornecedores",label:"FORNECEDORES",format:"currency"},{key:"investimentos_assumidos",label:"INVESTIMENTOS ASSUMIDOS",format:"currency"},{key:"outras_despesas_assumidas",label:"OUTRAS DESPESAS ASSUMIDAS",format:"currency"},{key:"dividas",label:"DÍVIDAS",format:"currency"},{key:"financiamentos",label:"FINANCIAMENTOS",format:"currency"},{key:"tributos_atrasados",label:"TRIBUTOS ATRASADOS",format:"currency"},{key:"acoes_processos",label:"AÇÕES E PROCESSOS",format:"currency"},{type:"empty"},{key:"saldo_qt_qd",label:"SALDO QT/QD",format:"currency",rowClass:"row-header"},{key:"indice_qt_qd",label:"ÍNDICE QT/QD",format:"number",rowClass:"row-header"},{key:"saldo_sem_dividas",label:"SALDO QT/QD SEM DÍVIDAS",format:"currency"},{key:"indice_sem_dividas",label:"ÍNDICE QT/QD SEM DÍVIDAS",format:"number"},{key:"saldo_sem_dividas_sem_estoque",label:"SALDO QT/QD SEM DÍVIDAS E SEM ESTOQUE",format:"currency"},{key:"prazo_medio_compra",label:"PRAZO MÉDIO DE COMPRA",format:"days"},{key:"prazo_venda",label:"PRAZO DE VENDA",format:"days"},{key:"pme",label:"PME",format:"days"},{key:"ciclo_financiamento",label:"CICLO DE FINANCIAMENTO",format:"days"},{type:"empty"},{type:"section",label:"INFORMAÇÕES COMPLEMENTARES"},{key:"faturamento_previsto_mes",label:"FATURAMENTO PREVISTO NO MÊS",format:"currency"},{key:"compras_mes",label:"COMPRAS NO MÊS",format:"currency"},{key:"entrada_mes",label:"ENTRADA NO MÊS",format:"currency"},{key:"venda_cupom_mes",label:"VENDA CUPOM NO MÊS",format:"currency"},{key:"venda_custo_mes",label:"VENDA CUSTO NO MÊS - CMV",format:"currency"},{key:"lucro_liquido_mes",label:"LUCRO LÍQUIDO NO MÊS",format:"currency"}];
const chartFieldCatalog=matrixRows.filter(r=>r.key).map(r=>({key:r.key,label:r.label,format:r.format||"currency"}));
const chartDefaults={weeks:12,months:3,years:2};
const chartState={range:"weeks",count:12,mode:"value",fields:["indice_qt_qd","saldo_qt_qd"]};
const form=document.getElementById("weeklyForm"),historyTable=document.getElementById("historyTable"),matrixTableWrap=document.getElementById("matrixTableWrap"),formCalculatedKpis=document.getElementById("formCalculatedKpis"),feedbackBox=document.getElementById("feedbackBox"),formModeBadge=document.getElementById("formModeBadge"),recordIdInput=document.getElementById("recordId"),connectionModeLabel=document.getElementById("connectionModeLabel"),chartFieldsGrid=document.getElementById("chartFieldsGrid"),chartRangeCountInput=document.getElementById("chartRangeCount"),chartPanelTitle=document.getElementById("chartPanelTitle");
let records=[],liquidityChartInstance=null,efficiencyChartInstance=null,financialTimelineChart=null;
const $=id=>document.getElementById(id);
const getRuntimeConfig=()=>window.QTQD_APP_CONFIG||{mode:"simulation",tenantId:""};
const isApiMode=()=>{const r=getRuntimeConfig();return r.mode==="api"&&!!r.tenantId&&!!window.QTQD_API_CLIENT};
const parseMoney=v=>Number.isFinite(Number(v))?Number(v):0;
const safeDivide=(a,b)=>b?a/b:null;
const fmtMoney=v=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(Number(v||0));
const fmtNum=v=>v===null||v===undefined||Number.isNaN(v)?"-":new Intl.NumberFormat("pt-BR",{maximumFractionDigits:2}).format(v);
const fmtDays=v=>v===null||v===undefined||Number.isNaN(v)?"-":`${fmtNum(v)} dias`;
const fmtDate=v=>new Intl.DateTimeFormat("pt-BR",{dateStyle:"short"}).format(new Date(`${v}T00:00:00`));
const avg=arr=>arr.length?arr.reduce((s,v)=>s+Number(v||0),0)/arr.length:0;
const initials=name=>String(name||"CL").split(" ").filter(Boolean).slice(0,2).map(p=>p[0].toUpperCase()).join("")||"CL";
function renderConnectionMode(){const r=getRuntimeConfig();connectionModeLabel.textContent=r.mode==="api"&&r.tenantId?`Modo conectado: API real (${r.tenantId})`:"Modo atual: simulação local"}
function getBranding(){try{return {...defaultBranding,...JSON.parse(localStorage.getItem(BRANDING_KEY)||"{}")}}catch{return {...defaultBranding}}}
function getTheme(){return localStorage.getItem(THEME_KEY)||"dark"}
function applyTheme(theme=getTheme()){document.body.dataset.theme=theme;document.querySelectorAll("[data-theme-choice]").forEach(b=>b.classList.toggle("active",b.dataset.themeChoice===theme))}
function applyBranding(){const b=getBranding(),name=b.clientName||defaultBranding.clientName;document.querySelectorAll("#clientSidebarName,#clientTopName").forEach(n=>n.textContent=name);document.querySelectorAll("#clientSidebarFallback,#clientTopFallback").forEach(n=>n.textContent=initials(name));document.querySelectorAll("#clientSidebarLogo,#clientTopLogo").forEach(n=>{if(b.clientLogoUrl){n.src=b.clientLogoUrl;n.classList.add("visible")}else{n.removeAttribute("src");n.classList.remove("visible")}});document.querySelectorAll("#clientSidebarFallback,#clientTopFallback").forEach(n=>n.style.display=b.clientLogoUrl?"none":"grid")}
function getFieldConfig(){try{return {...defaultFieldConfig,...JSON.parse(localStorage.getItem(FIELD_CONFIG_KEY)||"{}")}}catch{return {...defaultFieldConfig}}}
const isFieldVisible=k=>getFieldConfig()[k]?.visible!==false;
const getFieldLabel=(k,f)=>getFieldConfig()[k]?.label?.trim()||f;
function applyFieldConfigToForm(){const cfg=getFieldConfig();componentLabels.forEach(([k,l])=>{const input=$(k),field=input?.closest(".field"),span=field?.querySelector("span");if(span)span.textContent=cfg[k]?.label?.trim()||l;if(field)field.style.display=cfg[k]?.visible===false?"none":""})}
function createRecordFromValues(v){const qt=parseMoney(v.saldo_bancario)+parseMoney(v.contas_receber)+parseMoney(v.cartoes)+parseMoney(v.convenios)+parseMoney(v.cheques)+parseMoney(v.trade_marketing)+parseMoney(v.outros_qt)+parseMoney(v.estoque_custo);const qd=parseMoney(v.contas_pagar)+parseMoney(v.fornecedores)+parseMoney(v.investimentos_assumidos)+parseMoney(v.outras_despesas_assumidas)+parseMoney(v.dividas)+parseMoney(v.financiamentos)+parseMoney(v.tributos_atrasados)+parseMoney(v.acoes_processos);const saldo=qt-qd;const divs=parseMoney(v.dividas)+parseMoney(v.financiamentos)+parseMoney(v.tributos_atrasados)+parseMoney(v.acoes_processos);const pme=safeDivide(parseMoney(v.estoque_custo)*30,parseMoney(v.venda_custo_mes));const compra=safeDivide(parseMoney(v.contas_pagar)*30,parseMoney(v.compras_mes));const venda=safeDivide(parseMoney(v.contas_receber)*30,parseMoney(v.venda_cupom_mes));return {...v,qt_total:qt,qd_total:qd,saldo_qt_qd:saldo,indice_qt_qd:safeDivide(qt,qd),saldo_sem_dividas:saldo+divs,indice_sem_dividas:safeDivide(qt,qd-divs),saldo_sem_dividas_sem_estoque:saldo+divs-parseMoney(v.estoque_custo),prazo_medio_compra:compra,prazo_venda:venda,pme,ciclo_financiamento:(pme||0)+(venda||0)-(compra||0)}}
function seedRecords(){if(Array.isArray(window.QTQD_SEED)&&window.QTQD_SEED.length)return window.QTQD_SEED.map(i=>createRecordFromValues({...i,id:i.id||crypto.randomUUID()}));return[]}
function saveRecords(){localStorage.setItem(storageKey,JSON.stringify(records))}
function loadRecordsFromLocal(){const raw=localStorage.getItem(storageKey);if(!raw){records=seedRecords();saveRecords();return}try{records=JSON.parse(raw).map(i=>createRecordFromValues(i))}catch{records=seedRecords();saveRecords()}}
function extractComponentValues(record){const values={};componentLabels.forEach(([k])=>values[k]=parseMoney(record[k]));return values}
const apiRecordToLocal=api=>createRecordFromValues({id:api.id,weekDate:api.semana_referencia,status:api.status,...(api.valores||{})});
const localRecordToApi=record=>({tenant_id:getRuntimeConfig().tenantId,semana_referencia:record.weekDate,status:record.status,observacoes:null,...extractComponentValues(record)});
async function loadRecordsFromSource(){if(isApiMode()){records=(await window.QTQD_API_CLIENT.listAvaliacoes(getRuntimeConfig().tenantId)).map(apiRecordToLocal);saveRecords();return}loadRecordsFromLocal()}
function setFeedback(msg){feedbackBox.textContent=msg;feedbackBox.classList.remove("hidden")}
function clearFeedback(){feedbackBox.textContent="";feedbackBox.classList.add("hidden")}
function fillForm(record){recordIdInput.value=record.id;formModeBadge.textContent=`Editando ${fmtDate(record.weekDate)}`;$("weekDate").value=record.weekDate;$("recordStatus").value=record.status;componentLabels.forEach(([k])=>$(k).value=record[k]??"")}
function resetForm(){form.reset();recordIdInput.value="";formModeBadge.textContent="Nova semana";renderCalculatedPreview()}
function collectFormData(){const payload={id:recordIdInput.value||crypto.randomUUID(),weekDate:$("weekDate").value,status:$("recordStatus").value};componentLabels.forEach(([k])=>payload[k]=parseMoney($(k).value));return createRecordFromValues(payload)}
const getLatestRecord=()=>[...records].sort((a,b)=>b.weekDate.localeCompare(a.weekDate))[0]||null;
function renderCalculatedPreview(record){const r=record||collectFormData();const items=[["Saldo QT/QD",fmtMoney(r.saldo_qt_qd),r.saldo_qt_qd>=0?"good":"bad"],["Indice QT/QD",fmtNum(r.indice_qt_qd),(r.indice_qt_qd||0)>=1?"good":"bad"],["Saldo sem dividas",fmtMoney(r.saldo_sem_dividas),"neutral"],["Indice sem dividas",fmtNum(r.indice_sem_dividas),"neutral"],["Prazo medio de compra",fmtDays(r.prazo_medio_compra),"neutral"],["Prazo de venda",fmtDays(r.prazo_venda),"neutral"],["PME",fmtDays(r.pme),"neutral"],["Ciclo de financiamento",fmtDays(r.ciclo_financiamento),r.ciclo_financiamento<=0?"good":"bad"]];formCalculatedKpis.innerHTML=items.map(([l,v,t])=>`<article class="kpi-card ${t}"><span>${l}</span><strong>${v}</strong></article>`).join("")}
function renderHistory(){const ordered=[...records].sort((a,b)=>b.weekDate.localeCompare(a.weekDate));const latest=getLatestRecord();historyTable.innerHTML=ordered.map(r=>`<tr class="${latest&&r.id===latest.id?"latest-row":""}"><td>${fmtDate(r.weekDate)}</td><td>${r.status}</td><td>${fmtMoney(r.qt_total)}</td><td>${fmtMoney(r.qd_total)}</td><td>${fmtMoney(r.saldo_qt_qd)}</td><td>${fmtNum(r.indice_qt_qd)}</td><td><div class="action-row"><button class="action-btn" type="button" data-action="edit" data-id="${r.id}">Editar</button><button class="action-btn" type="button" data-action="delete" data-id="${r.id}">Excluir</button><button class="action-btn" type="button" data-action="evaluate" data-id="${r.id}">Avaliar</button></div></td></tr>`).join("")}
function fmtMatrix(format,v){if(format==="number")return fmtNum(v);if(format==="days")return fmtDays(v);return fmtMoney(v)}
function renderMatrix(){const ordered=[...records].sort((a,b)=>b.weekDate.localeCompare(a.weekDate));if(!ordered.length){matrixTableWrap.innerHTML="<p class='muted'>Nenhuma semana cadastrada.</p>";return}const visibleRows=matrixRows.filter(r=>!r.key||isFieldVisible(r.key));const template=`340px ${ordered.map(()=>`minmax(210px, 210px)`).join(" ")}`;const cells=[`<div class="matrix-cell matrix-head matrix-sticky">COMPONENTES</div>`].concat(ordered.map(r=>`<div class="matrix-cell matrix-head">${fmtDate(r.weekDate)}<br><span class="muted">${r.status}</span></div>`));visibleRows.forEach(row=>{if(row.type==="empty"){cells.push(`<div class="matrix-cell matrix-sticky row-empty"></div>`);ordered.forEach(()=>cells.push(`<div class="matrix-cell row-empty"></div>`));return}if(row.type==="section"){cells.push(`<div class="matrix-cell matrix-sticky row-section">${row.label}</div>`);ordered.forEach(()=>cells.push(`<div class="matrix-cell row-section"></div>`));return}const label=defaultFieldConfig[row.key]?getFieldLabel(row.key,row.label):row.label;cells.push(`<div class="matrix-cell matrix-sticky ${row.rowClass||""}">${label}</div>`);ordered.forEach(r=>cells.push(`<div class="matrix-cell ${row.rowClass||""}">${fmtMatrix(row.format,r[row.key])}</div>`))});matrixTableWrap.innerHTML=`<div class="matrix-scroll"><div class="matrix-grid" style="grid-template-columns:${template};">${cells.join("")}</div></div>`}
function buildInspectorModel(){const ordered=[...records].sort((a,b)=>a.weekDate.localeCompare(b.weekDate));if(!ordered.length)return null;const latest=ordered[ordered.length-1];return{ordered,latest,qt:ordered.map(i=>i.qt_total),qd:ordered.map(i=>i.qd_total),saldo:ordered.map(i=>i.saldo_qt_qd),indice:ordered.map(i=>i.indice_qt_qd||0),compra:ordered.map(i=>i.prazo_medio_compra||0),venda:ordered.map(i=>i.prazo_venda||0),pme:ordered.map(i=>i.pme||0),ciclo:ordered.map(i=>i.ciclo_financiamento||0)}}
function destroyInspectorCharts(){if(liquidityChartInstance)liquidityChartInstance.destroy();if(efficiencyChartInstance)efficiencyChartInstance.destroy();liquidityChartInstance=null;efficiencyChartInstance=null}
function renderInspector(){const m=buildInspectorModel();if(!m)return;const avgIndice=avg(m.indice),avgCompra=avg(m.compra),avgVenda=avg(m.venda),avgPme=avg(m.pme),avgCiclo=avg(m.ciclo);$("inspectorHero").innerHTML=[["Saldo QT/QD",fmtMoney(m.latest.saldo_qt_qd),m.latest.saldo_qt_qd>=0?"good":"bad"],["Indice QT/QD",fmtNum(m.latest.indice_qt_qd),(m.latest.indice_qt_qd||0)>=1?"good":"bad"],["Prazo medio de compra",fmtDays(m.latest.prazo_medio_compra),"neutral"],["Ciclo de financiamento",fmtDays(m.latest.ciclo_financiamento),m.latest.ciclo_financiamento<=0?"good":"bad"]].map(([l,v,t])=>`<article class="inspector-card inspector-metric ${t}"><span>${l}</span><strong>${v}</strong></article>`).join("");$("inspectorSemaphore").innerHTML=[["Liquidez",fmtNum(m.latest.indice_qt_qd),(m.latest.indice_qt_qd||0)>=1.1?"good":(m.latest.indice_qt_qd||0)>=1?"warning":"bad"],["Prazo compra",fmtDays(m.latest.prazo_medio_compra),(m.latest.prazo_medio_compra||0)>=45?"good":(m.latest.prazo_medio_compra||0)>=30?"warning":"bad"],["Prazo venda",fmtDays(m.latest.prazo_venda),(m.latest.prazo_venda||0)<=40?"good":(m.latest.prazo_venda||0)<=55?"warning":"bad"],["Ciclo",fmtDays(m.latest.ciclo_financiamento),(m.latest.ciclo_financiamento||0)<=0?"good":(m.latest.ciclo_financiamento||0)<=20?"warning":"bad"]].map(([l,v,s])=>`<article class="semaphore-item ${s}"><span>${l}</span><strong>${v}</strong></article>`).join("");$("inspectorBlocks").innerHTML=[`QT atual em ${fmtMoney(m.latest.qt_total)}.`,`QD atual em ${fmtMoney(m.latest.qd_total)}.`,`Prazo medio de compra em ${fmtDays(m.latest.prazo_medio_compra)} contra media de ${fmtDays(avgCompra)}.`,`Prazo de venda em ${fmtDays(m.latest.prazo_venda)} contra media de ${fmtDays(avgVenda)}.`,`PME em ${fmtDays(m.latest.pme)} contra media de ${fmtDays(avgPme)}.`,`Ciclo em ${fmtDays(m.latest.ciclo_financiamento)} contra media de ${fmtDays(avgCiclo)}.`].map(i=>`<div>${i}</div>`).join("");$("inspectorPeriods").innerHTML=[`Da primeira semana ${fmtDate(m.ordered[0].weekDate)} ate ${fmtDate(m.latest.weekDate)}, o saldo QT/QD saiu de ${fmtMoney(m.ordered[0].saldo_qt_qd)} para ${fmtMoney(m.latest.saldo_qt_qd)}.`,`O indice QT/QD passou de ${fmtNum(m.ordered[0].indice_qt_qd)} para ${fmtNum(m.latest.indice_qt_qd)}.`,`Prazo medio de compra em ${fmtDays(m.latest.prazo_medio_compra)} e prazo de venda em ${fmtDays(m.latest.prazo_venda)}.`,`Ciclo de financiamento em ${fmtDays(m.latest.ciclo_financiamento)}.`].map(i=>`<div>${i}</div>`).join("");$("inspectorNarrative").innerHTML=`<p>A semana mais recente fechou com QT de ${fmtMoney(m.latest.qt_total)}, QD de ${fmtMoney(m.latest.qd_total)}, saldo de ${fmtMoney(m.latest.saldo_qt_qd)} e indice QT/QD de ${fmtNum(m.latest.indice_qt_qd)}.</p>`;$("inspectorTrendTable").innerHTML=`<table><thead><tr><th>Indicador</th><th>Atual</th><th>Media</th></tr></thead><tbody>${[["QT total",fmtMoney(m.latest.qt_total),fmtMoney(avg(m.qt))],["QD total",fmtMoney(m.latest.qd_total),fmtMoney(avg(m.qd))],["Saldo QT/QD",fmtMoney(m.latest.saldo_qt_qd),fmtMoney(avg(m.saldo))],["Indice QT/QD",fmtNum(m.latest.indice_qt_qd),fmtNum(avgIndice)],["Prazo medio de compra",fmtDays(m.latest.prazo_medio_compra),fmtDays(avgCompra)],["Prazo de venda",fmtDays(m.latest.prazo_venda),fmtDays(avgVenda)],["PME",fmtDays(m.latest.pme),fmtDays(avgPme)],["Ciclo",fmtDays(m.latest.ciclo_financiamento),fmtDays(avgCiclo)]].map(([l,a,b])=>`<tr><td>${l}</td><td>${a}</td><td>${b}</td></tr>`).join("")}</tbody></table>`;$("inspectorRisks").innerHTML=[(m.latest.indice_qt_qd||0)<1?"Cobertura QT/QD abaixo do nivel de conforto.":null,(m.latest.ciclo_financiamento||0)>0?"Ciclo de financiamento positivo pressiona capital de giro.":null,(m.latest.prazo_venda||0)>(m.latest.prazo_medio_compra||0)?"Prazo de venda acima do prazo de compra aumenta tensao de caixa.":null,(m.latest.pme||0)>120?"PME elevado indica estoque com giro lento.":null].filter(Boolean).map(i=>`<div>${i}</div>`).join("")||"<div>Nao foram identificados riscos relevantes no recorte atual.</div>";$("inspectorActions").innerHTML=[`Manter acompanhamento semanal do saldo QT/QD e do indice QT/QD.`,`Negociar ganho de prazo medio de compra com fornecedores.`,`Acelerar o giro e o recebimento para reduzir o prazo de venda.`,`Atuar sobre o ciclo de financiamento, hoje em ${fmtDays(m.latest.ciclo_financiamento)}.`].map(i=>`<div>${i}</div>`).join("");$("inspectorDataTable").innerHTML=`<table><thead><tr><th>Semana</th><th>QT</th><th>QD</th><th>Saldo</th><th>Indice</th><th>Compra</th><th>Venda</th><th>PME</th><th>Ciclo</th></tr></thead><tbody>${m.ordered.map(i=>`<tr><td>${fmtDate(i.weekDate)}</td><td>${fmtMoney(i.qt_total)}</td><td>${fmtMoney(i.qd_total)}</td><td>${fmtMoney(i.saldo_qt_qd)}</td><td>${fmtNum(i.indice_qt_qd)}</td><td>${fmtDays(i.prazo_medio_compra)}</td><td>${fmtDays(i.prazo_venda)}</td><td>${fmtDays(i.pme)}</td><td>${fmtDays(i.ciclo_financiamento)}</td></tr>`).join("")}</tbody></table>`}
function generateInspectorCharts(){const m=buildInspectorModel();if(!m||!window.Chart)return;destroyInspectorCharts();const labels=m.ordered.map(i=>fmtDate(i.weekDate)),ink=getComputedStyle(document.body).getPropertyValue("--ink").trim(),muted=getComputedStyle(document.body).getPropertyValue("--muted").trim();liquidityChartInstance=new Chart($("liquidityChart"),{type:"line",data:{labels,datasets:[{label:"Prazo compra",data:m.compra,borderColor:"#4fd1c5",tension:.28},{label:"Prazo venda",data:m.venda,borderColor:"#ff8a7a",tension:.28},{label:"Ciclo",data:m.ciclo,borderColor:"#ffd166",tension:.28}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:ink}}},scales:{x:{ticks:{color:muted}},y:{ticks:{color:muted}}}}});efficiencyChartInstance=new Chart($("efficiencyChart"),{type:"bar",data:{labels,datasets:[{label:"QT",data:m.qt,backgroundColor:"rgba(79,209,197,.66)"},{label:"QD",data:m.qd,backgroundColor:"rgba(255,138,122,.66)"},{label:"Saldo",data:m.saldo,backgroundColor:"rgba(255,209,102,.66)"}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:ink}}},scales:{x:{ticks:{color:muted}},y:{ticks:{color:muted}}}}})}
async function generateInspectorPdf(){const m=buildInspectorModel();if(!m||!window.jspdf?.jsPDF){setFeedback("Nao foi possivel gerar o PDF no navegador atual.");return}const{jsPDF}=window.jspdf;const doc=new jsPDF({unit:"pt",format:"a4"});doc.setFont("helvetica","bold");doc.setFontSize(18);doc.text("QTQD - Relatorio Executivo",40,42);doc.setFont("helvetica","normal");doc.setFontSize(10);doc.text(`Emissao: ${new Date().toLocaleString("pt-BR")}`,40,66);doc.autoTable({startY:90,head:[["Indicador","Valor"]],body:[["QT",fmtMoney(m.latest.qt_total)],["QD",fmtMoney(m.latest.qd_total)],["Saldo QT/QD",fmtMoney(m.latest.saldo_qt_qd)],["Indice QT/QD",fmtNum(m.latest.indice_qt_qd)],["Prazo medio de compra",fmtDays(m.latest.prazo_medio_compra)],["Prazo de venda",fmtDays(m.latest.prazo_venda)],["PME",fmtDays(m.latest.pme)],["Ciclo",fmtDays(m.latest.ciclo_financiamento)]],theme:"grid",headStyles:{fillColor:[43,167,156]},margin:{left:40,right:40}});doc.save(`QTQD_Relatorio_${m.latest.weekDate}.pdf`)}
function setSidebarCollapsed(collapse){if(window.innerWidth<=1180){document.body.classList.remove("sidebar-collapsed","sidebar-open");return}document.body.classList.toggle("sidebar-collapsed",collapse);if(!collapse)document.body.classList.remove("sidebar-open")}
function openSection(id){document.querySelectorAll(".section-view").forEach(s=>s.classList.toggle("hidden",s.id!==id));document.querySelectorAll(".nav-link[data-section]").forEach(b=>b.classList.toggle("active",b.dataset.section===id));if(id==="painel")renderMatrix();if(id==="graficos")renderChartsPanel();if(id==="inspetor"){renderInspector();generateInspectorCharts()}setSidebarCollapsed(id!=="cadastro")}
function renderChartFieldOptions(){chartFieldsGrid.innerHTML=chartFieldCatalog.filter(f=>f.key==="saldo_qt_qd"||f.key==="indice_qt_qd"||isFieldVisible(f.key)).map(f=>`<label class="chart-field-option"><input type="checkbox" value="${f.key}" ${chartState.fields.includes(f.key)?"checked":""}><span>${getFieldLabel(f.key,f.label)}</span></label>`).join("")}
function aggregateRecords(range,count){const ordered=[...records].sort((a,b)=>a.weekDate.localeCompare(b.weekDate));if(range==="weeks")return ordered.slice(-count).map(i=>({label:fmtDate(i.weekDate),record:i}));const grouped=new Map();ordered.forEach(i=>{const d=new Date(`${i.weekDate}T00:00:00`);const key=range==="months"?`${String(d.getMonth()+1).padStart(2,"0")}/${d.getFullYear()}`:`${d.getFullYear()}`;grouped.set(key,i)});return Array.from(grouped.entries()).slice(-count).map(([label,record])=>({label,record}))}
function toPctSeries(values){const base=Number(values[0]||0);if(!base)return values.map(()=>0);return values.map(v=>((Number(v||0)-base)/Math.abs(base))*100)}
function destroyTimelineChart(){if(financialTimelineChart)financialTimelineChart.destroy();financialTimelineChart=null}
function renderChartsPanel(){renderChartFieldOptions();if(!window.Chart)return;const points=aggregateRecords(chartState.range,Math.max(1,Number(chartState.count)||chartDefaults[chartState.range]));const labels=points.map(p=>p.label),ink=getComputedStyle(document.body).getPropertyValue("--ink").trim(),muted=getComputedStyle(document.body).getPropertyValue("--muted").trim(),palette=["#4fd1c5","#ff8a7a","#ffd166","#7c9dfb","#8f6bff","#57cc99"];const fields=chartFieldCatalog.filter(f=>chartState.fields.includes(f.key));chartPanelTitle.textContent=fields.length?fields.map(f=>getFieldLabel(f.key,f.label)).join(" | "):"Selecione ao menos um campo";destroyTimelineChart();financialTimelineChart=new Chart($("financialTimelineChart"),{type:"line",data:{labels,datasets:fields.map((f,i)=>{const raw=points.map(p=>Number(p.record[f.key]||0));return{label:getFieldLabel(f.key,f.label),data:chartState.mode==="percent"?toPctSeries(raw):raw,borderColor:palette[i%palette.length],backgroundColor:`${palette[i%palette.length]}33`,tension:.28,fill:false}})},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:ink}}},scales:{x:{ticks:{color:muted}},y:{ticks:{color:muted,callback:v=>chartState.mode==="percent"?`${fmtNum(v)}%`:fmtNum(v)}}}}})}
function renderAll(){renderConnectionMode();applyFieldConfigToForm();renderHistory();renderMatrix();renderCalculatedPreview(getLatestRecord()||undefined);renderInspector();generateInspectorCharts();renderChartsPanel()}
form.addEventListener("submit",async e=>{e.preventDefault();clearFeedback();const record=collectFormData();if(!record.weekDate){setFeedback("Informe a data da semana antes de salvar.");return}const duplicate=records.find(i=>i.weekDate===record.weekDate&&i.id!==record.id);if(duplicate){setFeedback("Ja existe uma avaliacao cadastrada para esta semana.");return}const index=records.findIndex(i=>i.id===record.id);try{if(isApiMode()){if(index>=0){const api=await window.QTQD_API_CLIENT.updateAvaliacao(record.id,{status:record.status,valores:extractComponentValues(record)});records[index]=apiRecordToLocal(api);setFeedback("Semana atualizada na API com sucesso.")}else{const api=await window.QTQD_API_CLIENT.createAvaliacao(localRecordToApi(record));records.unshift(apiRecordToLocal(api));setFeedback("Semana cadastrada na API com sucesso.")}}else{if(index>=0){records[index]=record;setFeedback("Semana atualizada com sucesso.")}else{records.push(record);setFeedback("Semana cadastrada com sucesso.")}saveRecords()}}catch(error){setFeedback(`Falha ao salvar na API: ${error.message}`);return}saveRecords();renderAll();resetForm()});
$("resetFormButton").addEventListener("click",()=>{clearFeedback();resetForm()});
$("newEntryButton").addEventListener("click",()=>{openSection("cadastro");resetForm()});
$("evaluateButton").addEventListener("click",()=>{openSection("painel");setFeedback("Painel de avaliacao gerado com os dados salvos.")});
$("openInspectorButton").addEventListener("click",()=>{clearFeedback();openSection("inspetor")});
$("openGraphsButton").addEventListener("click",()=>{clearFeedback();openSection("graficos")});
$("refreshInspectorButton").addEventListener("click",()=>{renderInspector();generateInspectorCharts();setFeedback("Analise do Inspetor IA atualizada.")});
$("downloadPdfButton").addEventListener("click",()=>generateInspectorPdf());
$("seedDemoButton").addEventListener("click",()=>{if(isApiMode()){setFeedback("Recarregar base demo funciona apenas no modo de simulacao local.");return}records=seedRecords();saveRecords();renderAll();resetForm();setFeedback("Base inicial recarregada.")});
$("toggleFocusButton").addEventListener("click",()=>document.body.classList.add("focus-painel"));
$("backFromPanelButton").addEventListener("click",()=>document.body.classList.remove("focus-painel"));
document.querySelectorAll(".nav-link").forEach(b=>b.addEventListener("click",()=>{clearFeedback();openSection(b.dataset.section)}));
document.querySelectorAll("[data-theme-choice]").forEach(b=>b.addEventListener("click",()=>{localStorage.setItem(THEME_KEY,b.dataset.themeChoice);applyTheme(b.dataset.themeChoice);renderAll()}));
document.querySelectorAll("[data-chart-range]").forEach(b=>b.addEventListener("click",()=>{chartState.range=b.dataset.chartRange;chartState.count=chartDefaults[chartState.range];chartRangeCountInput.value=chartState.count;document.querySelectorAll("[data-chart-range]").forEach(x=>x.classList.toggle("active",x.dataset.chartRange===chartState.range));renderChartsPanel()}));
document.querySelectorAll("[data-chart-mode]").forEach(b=>b.addEventListener("click",()=>{chartState.mode=b.dataset.chartMode;document.querySelectorAll("[data-chart-mode]").forEach(x=>x.classList.toggle("active",x.dataset.chartMode===chartState.mode));renderChartsPanel()}));
chartRangeCountInput.addEventListener("input",()=>{chartState.count=Math.max(1,Number(chartRangeCountInput.value)||chartDefaults[chartState.range]);renderChartsPanel()});
chartFieldsGrid.addEventListener("change",e=>{const input=e.target.closest("input[type='checkbox']");if(!input)return;const selected=Array.from(chartFieldsGrid.querySelectorAll("input[type='checkbox']:checked")).map(i=>i.value);chartState.fields=selected.length?selected:["indice_qt_qd"];renderChartsPanel()});
const sidebarRevealButton=$("sidebarRevealButton");
function openSidebarPreview(){if(window.innerWidth>1180&&document.body.classList.contains("sidebar-collapsed"))document.body.classList.add("sidebar-open")}
function closeSidebarPreview(){document.body.classList.remove("sidebar-open")}
sidebarRevealButton.addEventListener("click",()=>{if(document.body.classList.contains("sidebar-open"))closeSidebarPreview();else openSidebarPreview()});
document.querySelector(".content")?.addEventListener("click",()=>{if(document.body.classList.contains("sidebar-collapsed"))closeSidebarPreview()});
historyTable.addEventListener("click",async e=>{const button=e.target.closest("[data-action]");if(!button)return;const record=records.find(i=>i.id===button.dataset.id);if(!record)return;if(button.dataset.action==="edit"){fillForm(record);openSection("cadastro");return}if(button.dataset.action==="evaluate"){fillForm(record);openSection("painel");setFeedback(`Avaliacao preparada para a semana ${fmtDate(record.weekDate)}.`);return}if(button.dataset.action==="delete"){try{if(isApiMode())await window.QTQD_API_CLIENT.deleteAvaliacao(record.id);records=records.filter(i=>i.id!==record.id);saveRecords();renderAll();resetForm();setFeedback(`Semana ${fmtDate(record.weekDate)} excluida.`)}catch(error){setFeedback(`Falha ao excluir na API: ${error.message}`)}}});
form.addEventListener("input",()=>renderCalculatedPreview());
window.addEventListener("resize",()=>{if(window.innerWidth<=1180)document.body.classList.remove("sidebar-collapsed","sidebar-open");renderChartsPanel();generateInspectorCharts()});
window.addEventListener("storage",e=>{if(e.key===FIELD_CONFIG_KEY)renderAll();if(e.key===BRANDING_KEY)applyBranding();if(e.key===THEME_KEY){applyTheme();renderAll()}});
async function initializeClient(){applyTheme();applyBranding();try{await loadRecordsFromSource()}catch(error){loadRecordsFromLocal();setFeedback(`Modo API configurado, mas foi mantido fallback local: ${error.message}`)}if(!records.length){records=seedRecords();saveRecords()}renderAll();const latest=getLatestRecord();if(latest){fillForm(latest);renderCalculatedPreview(latest)}else renderCalculatedPreview();openSection("cadastro")}
initializeClient();
